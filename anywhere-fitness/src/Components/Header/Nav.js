import React from 'react';

const Nav = props => {
    const { openLogin } = props;

    return (
        <>
            <h2>Nav Bar</h2>
            {/* <button onClick={openLogin}>Login/ Sign-UP</button> */}
        </>
    )
}

export default Nav